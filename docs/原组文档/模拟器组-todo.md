# 📋 项目修改待办清单

## ✅ 已确认的修改方向（全部已同步到 project-design.md 和 technical-framework.md）

### 1-10. （同前，事件数量/AI 策略/MBTI/自定义安全/选项显示/背景图/数据结构/激进属性/时间节奏/MIMO 配置 —— 均已定稿）

### 11. 初步成品已生成（2026-09-10）
- [x] 按技术框架搭建 `main/` 完整工程：server.js + common/ + simulator/
- [x] 全流程页面：录取通知书欢迎页 → 角色创建（含自评 MBTI）→ 48 事件循环 → 学期总结 → 毕业报告
- [x] AI 事件生成 + 预加载队列（含跨学期过期事件丢弃）+ 本地兜底事件池
- [x] 自定义选项（50 字限制 + 防注入声明 + 超纲幽默拒绝，不消耗回合）
- [x] 属性变化隐藏展示（选后才反馈）+ 数值强制钳制 [-25,+25] / [0,100]
- [x] 危机/巅峰事件倾向注入（属性 <10 / >95）
- [x] 纯 Canvas 雷达图（无外部依赖）+ 四年时间线 + 分享文案复制
- [x] 存档/读档/自动保存（LocalStorage，含 version 字段与 semesterStart 快照）
- [x] MiMo 关思考参数修正（见 bug.md 2026-09-10）

---

## 🔧 下一步待办

### P1（体验完善）
- [x] **属性权重实测微调**（2026-09-11）——已实现对称软边界平衡（`balanceDelta`），400 局蒙特卡洛：精力见底率 17%→2%，学业封顶率 47%→5%，且无单向漂移。调参脚本 `main/dev-balance-test.js`，改完系数可直接 `node dev-balance-test.js` 复测
- [ ] ~~AI 背景图~~（用户已决定不做，当前为 CSS 渐变 + emoji 兜底）
- [x] 事件提示词强化 `background` 枚举约束（2026-09-10：枚举强约束 + scene 长度强制，见 bug.md）
- [x] 读档已毕业态多生成事件（已修）
- [x] 学期总结页补齐「关键选择回顾」并分段排版（2026-09-11，对应设计文档 6.2）
- [x] **AI 返回 `+10` 导致 JSON 解析失败**（已修，见 bug.md 2026-09-11）

- [x] **危机 / 巅峰事件强制触发**（2026-09-11，用户反馈原实现只有提示词倾向、实际没体现）：属性 <10 → 危机事件（如精力枯竭病倒），学业/人际/心态 >95 → 巅峰事件；专属提示词 + 本地兜底池 + 同学期冷却防连环触发 + 事件徽章标识
- [x] 开场流程顺序（2026-09-11）：欢迎页按钮改为「填写新生信息」，创建页确认键才是「开启大学生活」
- [x] 自定义输入框残留上一题内容（2026-09-11）
- [x] 雷达图标签被画布裁切 + 4 条属性行的进度条长度不一致（2026-09-11）
- [x] **自定义行动判定放宽**（2026-09-11，用户反馈过严）：改为默认放行，仅拦截违法有害内容与超现实行为；解析失败也默认通过；收益单项封顶 10、正向总和封顶 18 防刷分
- [x] **事件类别分布均衡化**（2026-09-11，用户反馈学业占比过高）：类别不再由 AI 自选，改由系统按配额分配（学业 6 / 实践 7 / 宿舍 8 / 运动 9 / 社交 9 / 娱乐 9），配 `CATEGORY_HINTS` 给 AI 取材方向，同类事件不连续出现
- [x] **毕业去向文案与属性矛盾**（2026-09-11，用户反馈"精力 5 却写精力充沛"）：去向改由 `pickDestination()` 规则判定；提示词输出带档位的属性快照；新增禁词检测 + 重试 + 本地兜底文案（见 bug.md）
- [x] **交互优化三连**（2026-09-11，用户截图反馈）：① 移除键盘数字键 1-4 自动选中选项 ② 输入框每轮事件渲染前重置（此前已修，确认覆盖开场/普通/危机/巅峰全部路径）③ AI 文案打字机逐字输出（开场白/事件场景/选择反馈/学期总结/毕业结语，点击可跳过，长文本自动加速）
- [x] **打字机调速**（2026-09-11，用户反馈太快）：短文案每秒约 29 字，长文案平滑加速、总时长收敛 6 秒（TYPE_SLOW_MS / TYPE_FAST_MS / TYPE_TARGET_MS 三个常量可调）
- [x] **MBTI 自评简化**（2026-09-11，用户截图反馈）：16 型九宫格改为 I/E·N/S·T/F·J/P 四个维度大框，每项带大白话提示，实时显示已拼出的类型
- [x] **心态 / 人际只涨不跌、长期封顶**（2026-09-11，用户反馈）：实测 18 个事件中 72% 的三个选项全给心态正分，玩家没有下降入口。提示词加「取舍原则」硬约束 + 引擎新增 `statPressureHint()`（≥70 须提供下滑选项、≤30 须提供回升选项）。改前随机策略心态 99.8 / 人际 94.7，改后 44.0 / 34.3，≥85 占比从 100% 降到 4%（见 bug.md）
- [x] **「重新开始」按钮点了没反应**（2026-09-11，用户反馈）：根因是依赖浏览器原生 `confirm()`，在内嵌浏览器/沙箱环境被静默拦截后直接返回 false，代码 `if (!confirm()) return` 变成死键。改为页面内确认框 `confirmDialog()`；同时把重置逻辑重写为 `hardReset()`（清内存对局、待播特殊事件、打字动画、创建表单、MBTI），「再来一次」也走同一套（见 bug.md）
- [x] **视觉与排版细节**（2026-09-11，用户截图反馈）：场景背景出血撑满窗口宽、高度随视口自适应（16:10）；场景卡/选项悬停改浅描边、结语与成就卡改暖纸色渐变（去"AI 模板感"蓝）；长文本（临别赠言等）中文断行 + 两端对齐 + 行距 2.0；雷达图遮挡与进度条对齐此前已修，本轮复核仍在

### P2（可以有）
- [x] 成就系统（2026-09-11：10 枚徽章，毕业页展示 + 分享文案带解锁数）
- [ ] 性格测试开局
- [ ] 音效/BGM
- [ ] 移动端深度适配
- [ ] 在线部署（提交物可选）
- [ ] Demo 演示视频录制（3-5 分钟）

---

## 📝 运行方式

```bash
cd main && npm install
# .env 已创建（MIMO_API_KEY），勿提交
npm start   # → http://localhost:3000/simulator/
```
